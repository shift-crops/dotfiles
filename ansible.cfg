[defaults]
# Each component directory is treated as an Ansible role.
# "deps/" subdirectories (fonts, fzf, powerline) are also role roots.
roles_path        = .:deps
inventory         = inventory
retry_files_enabled = false
stdout_callback   = yaml
gathering         = smart
fact_caching      = memory

[privilege_escalation]
become_method = sudo
